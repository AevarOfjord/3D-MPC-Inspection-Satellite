# Linear C++

This directory holds the C++ backend used by the `cpp_linearized_rti_osqp` profile.

This profile reuses one frozen affine model across the prediction horizon before solving the OSQP-based QP.

For the mathematical formulation and paper-facing explanation, use `MATH/cpp_linearized_rti_osqp.md`.
